<%= form_for income, html: { class: "form-vertical center" } do |form| %>
  <% if income.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(income.errors.count, "error") %> prohibited this income from being saved:</h2>
      <ul>
        <% income.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-floating mb-3 mt-3">
    <div class="mb-3">
      <%= form.label :name, { class: "form-label" } %>
      <%= form.text_field :name, id: 'name', :class => "form-control" %>
    </div>
    <div class="mb-3">
      <%= form.label :description, { class: "form-label" } %>
      <%= form.text_field :description, id: 'description', :class => "form-control" %>
    </div>
    <div class="Action">
      <% if self.instance_variable_get(:@_controller).instance_variable_get(:@_action_name) != "new" %>
        <% if income.predefined == false %>
          <%= form.button "Update ", type: "submit", :class => "btn btn-outline-info" %>
        <% end %>
      <% else %>
        <%= form.button "Create ", name: 'save', type: "submit", :class => "btn btn-outline-info" %>
      <% end %>
    </div>
  </div>
<% end %>
