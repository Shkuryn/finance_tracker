<%= simple_form_for @new_invitation, method: :post do |f| %>

  <%= f.label :email, "Email for invitation" %>
  <%= f.text_field :email %>

  <%= f.button :submit, " Send ",class: "btn btn-outline-success" %>
<% end %>
